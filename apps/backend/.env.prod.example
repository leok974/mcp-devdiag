# DevDiag Backend Proxy - Production Configuration

# Required
DEVDIAG_BASE=https://devdiag-http.example.run.app
DEVDIAG_JWT=
DEVDIAG_TIMEOUT_S=120

# Optional
DEVDIAG_ENABLED=1

# Host allowlist (comma-separated)
# Root domains allow all subdomains + apex
# .ledger-mind.org allows: app.ledger-mind.org, ledger-mind.org, pr-123.ledger-mind.org
# For preview environments with patterns (pr-*.domain.com), use root domain (.domain.com)
DEVDIAG_ALLOW_HOSTS=.ledger-mind.org,app.ledger-mind.org
