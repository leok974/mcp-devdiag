# DevDiag Backend Proxy - Development Configuration

# Required
DEVDIAG_BASE=http://localhost:8080
DEVDIAG_JWT=
DEVDIAG_TIMEOUT_S=120

# Optional
DEVDIAG_ENABLED=1

# Host allowlist (comma-separated)
# Use root domains (.example.com) for wildcard subdomains
# Use exact hosts (app.example.com) for specific domains
DEVDIAG_ALLOW_HOSTS=.ledger-mind.org,app.ledger-mind.org,localhost,127.0.0.1
